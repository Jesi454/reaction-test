let start = 0
let go = 0
let goImg = images.createImage(`
    # # # # #
    # . . . #
    # . # . #
    # . . . #
    # # # # #
    `)
basic.forever(function () {
    start = randint(0, 10)
    if (start == 1) {
        let myImage: Image = null
        myImage.showImage(0)
    }
    if (go) {
    	
    }
})
